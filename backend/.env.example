# Copy this file to .env and fill in the values.
# .env is gitignored and must never be committed.

# Database — provide either DATABASE_URL or the individual variables.
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_NAME=postgres
DB_PASSWORD=gobank           # required when DATABASE_URL is not set
# DATABASE_URL=postgres://postgres:password@localhost:5432/postgres?sslmode=disable

# Server
LISTEN_ADDR=:3000

# JWT — required, must be a long random secret
JWT_SECRET=NhDmIdl2BeGZbwklk8pBv2yIBZWsfrC8tI4x3o993aWwo=            # required

# SMTP — optional; when unset, emails are only logged (useful for development)
SMTP_EMAIL=ahnaf.shahriar2003@gmail.com
SMTP_PASSWORD=oiyo jwqw bflo cgwa
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587

# Offers
COUPON_CODE=OFFER1000
